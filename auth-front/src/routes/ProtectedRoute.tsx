//auth-front/src/routes/ProtectedRoute.tsx
// ProtectedRoute.tsx
//import React from "react";
//import { Outlet, Navigate } from "react-router-dom";
//import { useAuth } from "../auth/AuthProvider"; // IMPORTAMOS EL CONTEXTO DE AUTENTICACIÓN

//export default function ProtectedRoute() {
  //const { user } = useAuth(); // OBTENEMOS EL USUARIO DESDE EL CONTEXTO
  //return user ? <Outlet /> : <Navigate to="/" />; // SI EL USUARIO ESTÁ AUTENTICADO,
  //  RENDERIZAMOS EL OUTLET, SINO REDIRECCIONAMOS A LOGIN
//}

